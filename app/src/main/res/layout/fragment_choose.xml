<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/llToolbar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/auth_toolbar_height"
        android:animateLayoutChanges="true"
        android:background="@color/bg_darkCerulean">

        <TextView
            android:id="@+id/ivTitle"
            style="@style/RobotoBoldWhiteXXXXXLargeTextStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/app_name"/>

        <android.support.design.widget.TabLayout
            android:id="@+id/tlTabAuth"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:layout_gravity="bottom"
            android:background="?attr/colorPrimary"
            app:tabIndicatorColor="@color/bg_white"
            app:tabSelectedTextColor="@color/text_white"
            app:tabTextAppearance="@style/RobotoMediumCapitalizeXSmallTextStyle"
            app:tabTextColor="@color/text_white_73"/>

    </FrameLayout>

    <android.support.v4.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/llToolbar"
        android:fillViewport="true">

        <com.rxlearning.utils.NonSwipeableViewPager
            android:id="@+id/vpAuth"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

    </android.support.v4.widget.NestedScrollView>

</RelativeLayout>